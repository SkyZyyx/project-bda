# ==============================================================================
# CONFIGURATION BACKEND - Plateforme d'Examens
# ==============================================================================
# Copiez ce fichier en .env et remplissez vos valeurs
# IMPORTANT : Ne jamais commiter le fichier .env !
# ==============================================================================

# Connexion PostgreSQL (Neon)
# Obtenez cette URL depuis votre dashboard Neon : https://console.neon.tech
DATABASE_URL=postgresql+asyncpg://user:password@hostname/neondb?ssl=require

# Clé secrète pour JWT
# Générez avec : openssl rand -hex 32
SECRET_KEY=changez-moi-en-production

# Configuration JWT
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS (origines autorisées)
CORS_ORIGINS=http://localhost:8501,http://localhost:3000

# Configuration API
API_V1_PREFIX=/api/v1
PROJECT_NAME=Plateforme Examens Universitaires
DEBUG=true
